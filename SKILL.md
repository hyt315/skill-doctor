---
name: skill-doctor
description: 审查任意 AI Agent 技能目录（SKILL.md/scripts/references），用四大架构形态适配（纯提示词/CLI工具/MCP协议端/多阶段流水线）、50+ 项静态规则、动态实跑、负向用例、40+ 条坑库对照找出流程走不通、口径矛盾、静默失效与安全泄露等问题，产出分级修复路径的审查报告。当用户要求审查/体检/优化某个技能、技能流程跑不通、或技能改动后需要回归验证时使用。
---

# 技能审查师（skill-doctor）

给任意技能目录做全身体检：找出**流程走不通、口径矛盾、静默失效与安全泄露**四类病，按分级修复路径打回。适用于 Claude Code / Codex / Cursor / DSH 等全形态 Agent 技能。

## 为什么需要它

静态读代码发现不了环境性缺陷（时间戳精度、Windows CRLF 编码、Token URL 泄露），设计者自查发现不了自己的盲区。有效审查必须四件套齐上：

1. **静态口径对齐**——文档、脚本、参考文件、多模态资产四方承诺对得上（借力 50+ 项工业级规则分类学 + Anthropic 官方规范，详见 `references/静态规则清单.md`；本技能自身零依赖，仅 Python 标准库）；
2. **动态实跑**——把技能自己的 selftest/校验脚本真跑一遍，报告可能是旧的，重跑才是当前真相；
3. **负向用例**——每道门必须构造“该 FAIL 的夹具”验证它真会拦；门在名义上存在、实际静默放行是最危险的病；
4. **40+ 条通病坑库对照**——拿历史踩坑逐条对照，不靠当场推理（详见 `references/坑库.md`）。

## Reference Files

三个核心参考文件承载本技能积累的审查资产：50+ 条规则的权威定义、四大形态审查流程、40+ 条真实踩坑。以下时机必须读取对应文件：

- `references/静态规则清单.md` —— 当 audit.py 报出 FAIL/WARN、需要写报告时：读对应规则的**定义与修复路径**，报告中引用规则必须注明出处；
- `references/审查方法论.md` —— 执行**步骤 0 形态判定、步骤 3 动态实跑、步骤 4 负向抽查**时：四大形态专属要点、selftest 作用域确认、破坏夹具的构造方法都在此文件；
- `references/坑库.md` —— **步骤 5 全程**：逐条对照每条踩坑的“检查方法”，能脚本化的已进 audit.py，其余靠人工判断。

## 审查工作流

```
审查进度：
- [ ] 步骤0：判定技能所属架构形态（纯提示词 / CLI工具 / MCP协议端 / 多阶段流水线）
- [ ] 步骤1：盘点技能目录结构（SKILL.md / scripts / references / assets 逐个登记）
- [ ] 步骤2：跑静态检查 scripts/audit.py <技能目录>（覆盖 50+ 项工业级规则）
- [ ] 步骤3：动态实跑（selftest 或技能自述的验证命令，加 --dynamic）
- [ ] 步骤4：负向用例抽查（对关键门构造破坏夹具）
- [ ] 步骤5：坑库对照（逐条过 references/坑库.md 的检查方法）
- [ ] 步骤6：汇总审查报告，输出 audit-report.txt 并给出分级修复路径
```

**步骤0 形态判定**：先读 `references/审查方法论.md` 的架构形态节，判定被审技能属于哪一类，锁定专属体检重点。

**步骤1 盘点**：列出技能全部文件（SKILL.md/scripts/references/assets 逐个登记），读 SKILL.md 了解其自述结构（阶段划分、产物清单、校验脚本名）。结构本身异常（嵌套 SKILL.md、引用不存在的文件）由步骤2 的静态检查抓。

**步骤2 静态**：`python <本技能>/scripts/audit.py <被审技能目录>`，覆盖八类检查（结构合规/引用资产/口径一致/静默失效/安全卫生/脚本工程/跨平台/动态实跑），规则与级别见 `references/静态规则清单.md`。行尾注释 `# skill-doctor: allow` 豁免单行误报；文件级启发式（SF002/SEC002/SEC004/PL001）用 `skill-doctor: allow-block 规则码` 豁免整文件。

**步骤3 动态**：`python <本技能>/scripts/audit.py <被审技能目录> --dynamic` 自动实跑被审技能的 selftest（默认不跑，防副作用；先确认其 selftest 只在临时目录/夹具上操作）。实跑覆盖：存在性、负向用例特征、退出码与结论一致性。

**步骤4 负向**：挑关键门（被审技能自己声称的硬门），故意构造违规输入验证门会拦。做负向抽查前，先读 `references/审查方法论.md` 的负向用例节——破坏夹具的构造方法在里面。门放行了该拦的东西 = 静默失效，最高优先级。

**步骤5 坑库**：逐条对照 `references/坑库.md`，每条坑都有“检查方法”，能脚本化的已并入 audit.py，需要人工判断的在此步过。

**步骤6 报告**：写 `audit-report.txt` 到被审技能目录（或通过 `--json` / `--markdown` 导出），格式：逐项检查结果（OK/WARN/FAIL + 证据）→ 问题清单（每项附最小修复路径）→ 整体策略（单项走最短路；多项并存取最深根因）。

## 自审与互审

- **改本技能后**：`python scripts/selftest.py`（负向夹具逐规则回归）+ `python scripts/trigger_eval.py`（触发边界回归），两者全绿才算完；
- **改 description**：必须过 `evals/trigger_cases.json` 全家族用例；新增触发场景同步补用例进该文件；
- **治理元数据**：`manifest.json` 与 `CHANGELOG.md`，版本随功能演进递增。

## 分级修复决策

打回不搞一律重做：
- **单项问题**：按该项最小修复路径处理（如仅引用断链→补文件即可，不用动流程）；
- **多项并存**：取最深根因（多数表面问题常是同一根因的多个症状），从根因处修起；
- **判断口径**：修复后必须重跑 audit 全绿才算完；负向用例发现的新行为要回写坑库。

## 审查纪律

- 审查者不得顺手修被审技能——报告归报告，修复是另一个明确授权的动作；
- 每条 FAIL 必须附证据（文件行号、命令输出、复现步骤），不写“感觉有问题”；
- 报告中引用的每条规则/坑注明出处文件（静态规则清单/坑库）；未读对应文件不得标记该审查步骤完成；
- 不确定的降级为 WARN 并写明验证方法，不拿猜测当 FAIL；
- 审查报告不含被审技能的敏感内容（密钥、账号、私有路径、Token URL）。
