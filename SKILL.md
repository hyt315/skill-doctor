---
name: skill-doctor
description: 审查与优化 AI Agent 技能目录（SKILL.md、scripts、references）。当用户要求技能体检、迭代改进、排查流程跑不通或静默失效、改动后回归验证时使用。提供静态规则扫描、经授权的动态自测、形态适配、SEI 成熟度参考与可验证的修复方案。不用于普通代码 review、文章改写或与技能无关的性能优化。
---

# 技能审查与进化（skill-doctor）

目标是让技能更可靠、更易执行，而不是堆叠文档、测试或提高分数。采用「诊断 → 方案 → 修改 → 验证」四阶段流程，深度随任务风险调整。

## 先确定边界

1. 从用户指定路径或当前仓库定位目标技能，确认 `SKILL.md`；多个候选无法区分时再询问。
2. 区分请求：**体检/找问题**只读交付；**优化/修复**可以修改相关文件；**提交/推送**需要用户明确授权。已有授权不重复询问，删除资产或大范围重写另列风险与回滚方案。
3. 记录工作区已有改动，不覆盖、不清理用户文件。先检查动态入口，再决定是否执行。
4. 以下命令从 **skill-doctor 自身目录**执行，`<目标目录>`替换为真实路径并加引号；切换到目标仓库后使用本技能脚本的绝对路径，避免误跑同名脚本。

## 按需读取参考文件

- **出现 FAIL/WARN 或解释规则时**：读取 [静态规则清单](references/静态规则清单.md) 对应规则的级别、定义与修复路径。区分硬错误、启发式告警和人工检查项。
- **涉及安全、状态持久化、跨平台或静默失败时**：读取 [坑库](references/坑库.md) 中相关条目，按“检查方法”核验，不默认逐条复查无关问题。
- **需要形态判定、深入检索、负向夹具、SEI 解读或完整报告时**：读取 [审查与进化方法论](references/审查与进化方法论.md) 对应章节。

## 阶段 1：只读诊断

- 盘点 `SKILL.md`、脚本、引用和测试，识别 PROMPT / CLI / MCP / PIPELINE / HYBRID 形态。
- 执行一次 `python scripts/audit.py "<目标目录>" --json`，保留基线结果。默认不生成 `audit-report.txt`；`--stdout` 仍兼容。
- 针对报告中的问题读源码，记录文件位置、触发条件和复现证据；规则命中本身不是已证实的漏洞。
- 动态验证只在入口已审阅、环境安全且用户授权覆盖时执行：`python scripts/audit.py "<目标目录>" --dynamic --timeout 120`。它会运行目标代码，**不是沙箱**，可能写文件、联网或修改环境。未知代码先用独立受限环境。
- `evals/`、`tests/`、Makefile 被发现不等于已经执行；按实际入口运行，或标明未执行。负向关键词存在不等于负向拦截有效。
- 仅体检时在这里交付并结束；默认给关键缺陷与未验证项，用户要完整报告时才用 `--report`，需要归档才加 `--report-file <路径>`。

## 阶段 2：选择最小有效方案

- 按影响排序：安全与数据风险 → 错误结果/流程阻断 → 可维护性与使用成本。优先修复有复现证据的问题。
- 大范围演进时运行 `python scripts/evolve.py "<目标目录>" --plan`；需要成熟度基线时运行 `--analyze`。SEI 是结构启发式参考，不是业务正确性证明，也不是发布硬门。
- 仅当外部规范、依赖行为或同类实现影响决策时检索。`--research-plan` 提供 12 组候选查询，按相关性选用，不强制全部执行。显式持久化用 `--research-plan --write-task`。
- 使用宿主实际提供的搜索与网页读取工具，精读相关原始来源，记录 URL、适用版本、结论与未确认事项；没有联网能力就说明，不虚构检索结果。
- `--offline-fallback` 会写入启发式避坑草稿与任务状态，仅在需要该资产且有修改授权时运行。它不代表研究完成，不是修复本地明确缺陷的前置条件。

## 阶段 3：修改与治理

- 将重复或模糊指令改成“何时做、做什么、如何判断结果”，公共细节集中到参考文件；只修复影响执行的弱引用，不批量机械改写措辞。
- 对真实依赖添加产物存在性、内容有效性或状态断言；仅有工单 JSON 不能证明研究完成或下游已被阻断。
- 优先扩展已有测试。缺少测试入口时，CLI/HYBRID 可用 `--scaffold-test`，PROMPT 可用 `--scaffold-prompt`；`--scaffold-all` 仅在确需整套资产时使用。脚手架是起点，需要补领域用例和引用。
- 写操作显式开启；`--force` 会覆盖生成文件，先展示覆盖清单再征求确认。审计报告、计划、脚手架不得覆盖用户原有成果。
- 保持 Python 3.10+ 标准库零依赖；不为纯提示词技能添加无用脚本。

## 阶段 4：按风险验证并交付

- 对改动路径运行必要验证：一个代表性正向用例、对应缺陷的负向回归、涉及的语法/配置检查。大改或共享规则变化再扩大覆盖，复用未受影响的结果。
- 复跑静态审查，对比新增/消除的 FAIL 与 WARN；解释确认的误报和未修复项，不以增加豁免、空测试或堆关键词凑全绿。
- 修改本技能时执行 `python -B -m unittest discover -s tests -v`，其中 `tests/test_skill.py` 会运行 `scripts/selftest.py`，后者已包含 `scripts/trigger_eval.py` 的触发边界回归，不重复跑同一套。
- 检查 diff 与工作区，仅移除本次生成且确认无用的临时文件；正式交付报告不当作垃圾清理。功能和兼容性变化同步 `manifest.json`、`CHANGELOG.md` 与中英文 README。
- 版本与上传准备优先结合已安装的 **github-oss-prep**：读取其 Agent Skills 分发与发布门禁章节，核对版本来源、日志、安装说明和待推送文件；没有安装时按本仓库清单完成，不添加强制依赖。源码推送、Tag、Release 分开处理。
- 提交/推送按用户授权执行，不强推、不自动发布 Release。完成后提供提交链接与实际验证结果。

## 交付格式

默认用简短结论加三项：**改了什么、验证了什么、还未覆盖什么**；未执行写 `NOT_RUN`，请求后不适用写 `SKIPPED`，实际失败写 `FAIL`，不拿静态通过代替动态成功。完整体检才展开方法论中的分层事实卡与报告。

示例：修复动态报告误报。失败自测现报告 FAIL；未请求动态测试时为 NOT_RUN。已通过针对性负向回归；未验证目标技能外部服务。仅在拿到对应证据后才能使用这类结论。
